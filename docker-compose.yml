version: '3'

services:
  sqs:
    image: softwaremill/elasticmq
    ports:
      - 9324:9324
      - 9325:9325
    volumes:
    - type: bind
      source: ./sqs/custom.conf
      target: /opt/elasticmq.conf